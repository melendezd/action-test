on:
  workflow_call:
    inputs:
      environment:
        type: string
        required: true

jobs:
  runTest:
    name: run test
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    steps:
      - name: print one
        shell: bash
        run: echo "${{ inputs.environment }}"
