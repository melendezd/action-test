on:
  workflow_call:
    inputs:
      path:
        required: true
        type: string
      val:
        required: true
        type: string

jobs:
  job1:
    uses: melendezd/action-test/.github/workflows/called2.yml@test-call
      with: 
        val: inputs.val
