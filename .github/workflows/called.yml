on:
  workflow_call:
    inputs:
      path:
        required: true
        type: string
      val:
        required: true
        type: string

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - uses: ${{ inputs.path }}
        with: 
          val: inputs.val
